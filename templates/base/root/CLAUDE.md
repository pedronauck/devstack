# {{projectTitle}}

This repository was generated by `@compozy/devstack`.

## Working Agreement

- Finish work only after `make check` passes.
- Fix production issues revealed by tests instead of weakening the tests.
- Prefer root-cause fixes over workarounds.
- Keep changes reusable and product-agnostic until the domain is intentionally modeled.
- Replace starter copy, routes and examples as soon as real product workflows exist.

## Commands

```bash
bun install
docker compose up -d
bun run dev

bun run lint
bun run typecheck
bun run test
make check
```

## Workspace Layout

- `packages/frontend`: React 19 + Vite + TanStack Router + TanStack Query + Tailwind v4
- `packages/backend`: Hono + Drizzle + PostgreSQL + Zod OpenAPI helpers
- `.github`, `.husky`, `Makefile`: shared quality and automation entrypoints

## Development Notes

- Use placeholders such as `{{projectName}}` and `{{projectTitle}}` only inside the generator templates, never in generated app code after scaffolding.
- Keep the app shell generic until feature modules, auth and infra choices are finalized.
- Treat CI, commit hooks and the Makefile as the default contract for quality gates.

## Module Notes

<!-- MODULE_SECTIONS -->
